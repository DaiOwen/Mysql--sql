（1）、增
INSERT [INTO] 表名 [(字段列表)] VALUES (值列表)[,(值列表)，...]
 --如果要插入的值列表包含所有字段列表并且顺序一致，则可以省略字段列表
 --可同时插入多条数据记录
 --字段列表可以用“*” 代替，代替所有字段。
 （2）、删
 DELETE FROM 表名[删除条件子句]
 没有条件子句，则删除所有数据
 （3）、改
 UPDATE 表名 SET 字段名 = 新值[,字段名 = 新值][更新条件]
